# Bienvenue dans ce cours consacrer au language dart

## **Somaires**

### 1. **Installation**

### 2. **Les commentaires**

a. uniligne `//`
b. mutiligne `/* */`
c. Documentation uniligne `///`
d. Documentation multiligne `/* ** */`

### 3. **Les variables**

a. Le mot clé( `var` )
b. Typée( `String, int, double, etc...` )
c. Les constantes( `final`, `const` )
d. Le Type Dynamique( `dynamic` )
e. Les Assignations retarder( `late` )

### 4. **Les opérateurs**

a. Arithmétiques( `+`, `-`, `*`, `/`, `~/`, `%` )
b. Relationnels( `==`, `<`, `>`, `<=`, `>=`, `!=` )
c. Test types( `is`, `is!`, `as` )
d. Bit à Bit( `&`, `|`, `<<`, `>>`, `^` )
e. Assignations( `=`, `+=`, `-=`, `*=`, `/=`, `~/`, `%`, `??=` )
f. Logical( `&&`, `||`, `!`, `??` )
g. Null Safety( `?`, `!` )
h. Cascade( `..` )

### 5. **Les entrées et sorties standard**

a. Les entrées

* `stdin.readLineSync()`
* `stdin.readByte()`
b. Les sorties
* `print()`
* `stdout.write()`

### 6. **Les types**

a. Les nombres( `num`, `int`, `double` )
b. Les chaines de caractères( `String` )
c. Les Lists ( `List` )
d. Les Sets ( `Set` )
e. Les Maps ( `Map` )
f. Les Runes ( `Runes` )
g. Les Symbols ( `Symbol` )
h. Le Null ( `Null` )

### 7. Les fonctions

a. Déclaration
b. Les Paramètres

* Optinnels
* Nommés
* Avec valeur par défaut
c. Fonction récusive
d. Lambda expression
e. Les Types de Fonction
f. Les Fonctions anonymes
g. Sortir lors de l'exécution du code
h. Les fonctions asynchrone ( `await`, `async`, `Future` )

### 8. Les classes

a. Déclaration( `class` )
b. Les Attributs
c. Les Méthodes
d. Les Constructeurs

* Par défaut
* Personnaliser
* Multiples
* Le mot clé `super`
e. Le mot clé `this`
f. Le mot clé `static`
g. L'héritage
* Avec `extends`
* Les Mixin `mixin`
h. Le Overriding `@override`
i. Les accesseurs
* Getters `get`
* Setters `set`
j. Les classes abstraite `astract`
k. Les classes comme des fonctions `call`
l. Les interfaces
m. Déclaration

### 9. Les exceptions

a. Le Block try-catch ( `try`, `catch` )
b. On ( `On` )
c. Les Exceptions Personnaliser

### 10. Les assertions
